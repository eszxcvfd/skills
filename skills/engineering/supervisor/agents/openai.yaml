interface:
  display_name: "Supervisor"
  short_description: "Macro decision and momentum proxy above root"
  default_prompt: "Use $supervisor to act as the human's macro decision proxy: resolve requirements/architecture/scope, audit root plan/progress, decide APPROVED/REVISE/RECOVER/ESCALATE, recover lost momentum from history, and report evidence-backed progress without planning peer work."
policy:
  allow_implicit_invocation: false
